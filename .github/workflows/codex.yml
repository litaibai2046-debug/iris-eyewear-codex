name: Codex Lint/Test  
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Xcode environment
        run: echo "Using default macOS runner"
      - name: Build project
        run: echo "Running placeholder build - replace with xcodebuild"
